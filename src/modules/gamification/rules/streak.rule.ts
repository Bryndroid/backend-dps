// Reglas para conservar las rachas, repararlas etc. Como lo implemento?

export enum STRIKE_CONFIG {
    MAX_DAYS = 2,
    MIN_DAYS = 0,
    RESET_STRIKE = 1,
    DECREMENT_STRIKE = 1,
    AUMENT_STRIKE = 1
}